{
  "private": true,
  "scripts": {
    "deploy": "surge ./public --domain week-9-monday-warm-up.bdiboll.surge.sh",
    "start": "browser-sync start --server 'public' --files 'public'"
  },
  "dependencies": {
    "browser-sync": "^2.24.1",
    "stylelint": "^9.2.0",
    "stylelint-config-standard": "^18.2.0",
    "surge": "^0.20.1"
  }
}
